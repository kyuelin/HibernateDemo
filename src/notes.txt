1. add org.hibernate.tool.hbm2ddl.SchemaExport to troubleshoot hibernate schema issue;
2. install mysql
create database;
create user;
3. git best practice
git checkout master
git pull origin master
git branch local
git checkout local
git merge master

# edited
git push origin local
# create pull reqeust to merge local into master
# commit merged codes from remote local to remote mater

